version: '3.8'
services:
  mongo:
    image: mongo
  
  test:
    depends_on:
      - mongo
    build:
      context: ..
      dockerfile: .docker/tag-test.Dockerfile
      args:
        TAG: mongostore
    environment:
      - MONGO_URL=mongodb://mongo:27017
